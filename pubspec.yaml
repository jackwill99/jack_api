name: jack_api
description: Flutter package for api source and network on the top of Dio package
version: 0.0.1
homepage:

environment:
  sdk: ">=3.0.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  cryptography:
  dio:
  dio_http2_adapter:
  flutter:
    sdk: flutter
  get_it:
  isar: ^3.1.0+1
  isar_flutter_libs:
  path_provider:
  pretty_dio_logger:

dev_dependencies:
  build_runner:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  isar_generator:
  lints: ^2.1.1

flutter:
